const app=document.getElementById("app"),nav=document.getElementById("nav"),toastEl=document.getElementById("toast");
const state={token:localStorage.getItem("ajv_token"),user:JSON.parse(localStorage.getItem("ajv_user")||"null"),role:"student",staffStudents:[],courses:[]};
const esc=s=>String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[m]));
function toast(msg,ok=true){toastEl.textContent=msg;toastEl.className=ok?"show good":"show bad";setTimeout(()=>toastEl.className="",3500)}
async function api(url,opt={}){opt.headers={...(opt.headers||{})};if(opt.body&&typeof opt.body!=="string")opt.body=JSON.stringify(opt.body);if(opt.body)opt.headers["Content-Type"]="application/json";if(state.token)opt.headers.Authorization="Bearer "+state.token;const r=await fetch(url,opt);const d=await r.json().catch(()=>({}));if(r.status===401){logout(false);throw Error(d.message||"Session expired. Please login again.")}if(!r.ok)throw Error(d.message||"Request failed");return d}
function setAuth(d){state.token=d.token;state.user=d.user;localStorage.setItem("ajv_token",d.token);localStorage.setItem("ajv_user",JSON.stringify(d.user));layoutNav()}
function logout(show=true){state.token=null;state.user=null;localStorage.removeItem("ajv_token");localStorage.removeItem("ajv_user");layoutNav();showPage("home");if(show)toast("Logged out successfully")}
function layoutNav(){nav.innerHTML=state.user?`<button onclick="showPage('dashboard')">Dashboard</button><button onclick="showPage('profile')">Profile</button>${state.user.role!=="student"?`<button onclick="loadStudents()">Students</button>`:""}<button onclick="logout()">Logout</button>`:`<button onclick="showPage('home')">Home</button><button onclick="showPage('login')">Login</button><button class="nav-register" onclick="showPage('register')">Register</button>`}
function home(){app.innerHTML=`<section class="hero"><div class="hero-copy"><div class="eyebrow">AJV COLLEGE OF ENGINEERING</div><h1>One campus.<br><em>One connected</em> experience.</h1><p>A secure college portal for student registration, academic records, attendance, marks, percentage, grades and CGPA — with staff-controlled academic updates.</p><div class="actions"><button class="btn" onclick="showPage('login')">Open College Portal</button><button class="btn gold" onclick="showPage('register')">Student Registration</button></div><div class="trust"><span>✓ Automatic Login ID</span><span>✓ Staff mark entry</span><span>✓ Live CGPA</span></div></div><div class="hero-card"><div class="hero-visual"><img src="/assets/college-logo.png"><h2>CollegeConnect</h2><p>Precision in every design</p><div class="hero-badges"><span>Student</span><span>Staff</span><span>Academic</span></div></div></div></section><section class="section"><div class="eyebrow">CORE COLLEGE WORKFLOW</div><h2>Simple for students. Powerful for staff.</h2><p class="muted">The portal keeps the important academic flow connected from registration to final CGPA.</p><div class="grid"><div class="card"><div class="icon">🎓</div><h3>Student Registration</h3><p class="muted">Mandatory identity details are stored securely and a unique login ID is generated automatically.</p></div><div class="card"><div class="icon">📚</div><h3>Academic Records</h3><p class="muted">Students can see subjects, attendance, internal, external, total percentage, grade and grade point.</p></div><div class="card"><div class="icon">📊</div><h3>Staff Mark Entry</h3><p class="muted">Authorized staff select a student and subject. The system calculates percentage, grade and CGPA automatically.</p></div></div></section><section class="workflow"><div><div class="eyebrow">ACADEMIC FLOW</div><h2>Register → Login → Enter Marks → Results</h2><p class="muted">No manual CGPA calculation. The backend recalculates the student's academic summary every time marks are saved.</p></div><div class="flow"><span>REGISTER</span><b>→</b><span>LOGIN ID</span><b>→</b><span>MARKS</span><b>→</b><span>GRADE</span><b>→</b><span>CGPA</span></div></section>`}
function login(){state.role="student";renderLogin()}
function renderLogin(){app.innerHTML=`<div class="login-wrap"><div class="login"><div class="login-head"><img src="/assets/college-logo.png"><div class="eyebrow">SECURE PORTAL</div><h1>Welcome back</h1><p class="muted">Sign in to AJV CollegeConnect</p></div><div class="tabs"><button id="studentTab" class="${state.role==='student'?'active':''}" onclick="setLoginRole('student')">Student</button><button id="staffTab" class="${state.role!=='student'?'active':''}" onclick="setLoginRole('staff')">Staff</button><button onclick="showPage('register')">Register</button></div><form onsubmit="doLogin(event)"><label>Login ID</label><input id="loginId" placeholder="e.g. AJVSTU001" required><label>Password</label><input id="password" type="password" placeholder="Enter password" required><div class="notice">Demo: ${state.role==='student'?"AJVSTU001 / Student@123":"FAC001 / Faculty@123"}</div><button class="btn full">Secure Sign In →</button></form></div></div>`}
function setLoginRole(r){state.role=r;renderLogin()}
async function doLogin(e){e.preventDefault();try{const d=await api("/api/auth/login",{method:"POST",body:{loginId:loginId.value,password:password.value,role:state.role}});setAuth(d);toast("Welcome, "+d.user.fullName);showPage("dashboard")}catch(x){toast(x.message,false)}}
async function register(){let cfg={departments:["Information Technology","Computer Science and Engineering","Electronics and Communication Engineering","Electrical and Electronics Engineering","Mechanical Engineering","Artificial Intelligence and Data Science"],years:["I Year","II Year","III Year","IV Year"]};try{cfg=await api("/api/config")}catch{}app.innerHTML=`<div class="register-wrap"><div class="register-card"><div class="register-head"><img src="/assets/college-logo.png"><div><div class="eyebrow">STUDENT ONBOARDING</div><h1>Create your college identity</h1><p class="muted">All fields marked * are mandatory. Your Login ID is generated automatically after registration.</p></div></div><form onsubmit="doRegister(event)" class="form-grid"><label>Full Name *<input id="rName" required></label><label>Date of Birth *<input id="rDob" type="date" required></label><label>Gender *<select id="rGender" required><option value="">Select</option><option>Female</option><option>Male</option><option>Other</option></select></label><label>Parent / Guardian Name *<input id="rParent" required></label><label>Email *<input id="rEmail" type="email" required></label><label>Phone *<input id="rPhone" inputmode="numeric" pattern="[0-9]{10}" maxlength="10" required></label><label>Department *<select id="rDept" required>${cfg.departments.map(x=>`<option>${esc(x)}</option>`).join("")}</select></label><label>Year *<select id="rYear" required>${cfg.years.map(x=>`<option>${esc(x)}</option>`).join("")}</select></label><label>Section *<select id="rSection" required><option>A</option><option>B</option><option>C</option></select></label><label>Password *<input id="rPass" type="password" minlength="6" required></label><label class="span2">Address *<textarea id="rAddress" rows="3" required></textarea></label><div class="span2 form-actions"><button type="button" class="btn secondary" onclick="showPage('login')">Back to Login</button><button class="btn gold">Register Student →</button></div></form></div></div>`}
async function doRegister(e){e.preventDefault();try{const d=await api("/api/students/register",{method:"POST",body:{fullName:rName.value,dob:rDob.value,gender:rGender.value,parentName:rParent.value,email:rEmail.value,phone:rPhone.value,department:rDept.value,year:rYear.value,section:rSection.value,address:rAddress.value,password:rPass.value}});app.innerHTML=`<div class="success-wrap"><div class="success-card"><div class="success-icon">✓</div><div class="eyebrow">REGISTRATION SUCCESSFUL</div><h1>Welcome, ${esc(d.studentName)}!</h1><p class="muted">Your student account has been created. Save the Login ID below.</p><div class="login-ticket"><span>YOUR LOGIN ID</span><strong>${esc(d.loginId)}</strong><small>Use this ID with the password you created.</small></div><button class="btn full" onclick="prefillLogin('${esc(d.loginId)}')">Continue to Student Login →</button></div></div>`;toast("Student registered successfully")}catch(x){toast(x.message,false)}}
function prefillLogin(id){state.role="student";renderLogin();setTimeout(()=>{loginId.value=id},0)}
async function studentDashboard(){try{const d=await api("/api/student/dashboard");app.innerHTML=`<div class="dashboard"><div class="dash-head"><div><div class="eyebrow">STUDENT PORTAL</div><h1>Hello, ${esc(d.profile.fullName.split(" ")[0])} 👋</h1><p class="muted">${esc(d.profile.loginId)} · ${esc(d.profile.department)} · ${esc(d.profile.year)} · Section ${esc(d.profile.section)}</p></div><span class="status">● Active Student</span></div><div class="stats"><div class="stat"><span class="label">CGPA</span><b>${Number(d.academic.cgpa).toFixed(2)}</b><small>Credit weighted</small></div><div class="stat"><span class="label">Overall Percentage</span><b>${Number(d.academic.overallPercentage).toFixed(2)}%</b><small>All recorded subjects</small></div><div class="stat"><span class="label">Attendance</span><b>${Number(d.academic.attendance).toFixed(1)}%</b><small>Average attendance</small></div><div class="stat"><span class="label">Courses</span><b>${d.courses.length}</b><small>Academic records</small></div></div><div class="profile-strip"><div><span>Student ID</span><b>${esc(d.profile.loginId)}</b></div><div><span>Email</span><b>${esc(d.profile.email)}</b></div><div><span>Phone</span><b>${esc(d.profile.phone)}</b></div><div><span>Parent</span><b>${esc(d.profile.parentName||"—")}</b></div></div><div class="table-wrap"><div class="table-title"><div><strong>Marks & Academic Results</strong><span class="muted">Live records entered by staff</span></div></div><table><thead><tr><th>Subject</th><th>Credits</th><th>Attendance</th><th>Internal / 40</th><th>External / 60</th><th>Total / 100</th><th>Grade</th><th>Point</th></tr></thead><tbody>${d.courses.length?d.courses.map(x=>`<tr><td><b>${esc(x.course.code)}</b><br>${esc(x.course.name)}</td><td>${x.course.credits}</td><td>${x.attendance}%</td><td>${x.internalMark}</td><td>${x.externalMark}</td><td><b>${x.totalMark}%</b></td><td><span class="pill">${esc(x.grade)}</span></td><td>${x.gradePoint}</td></tr>`).join(""):`<tr><td colspan="8">No marks have been entered by staff yet.</td></tr>`}</tbody></table></div><div class="two-col"><div class="card"><h3>Student Identity</h3><div class="detail-grid"><span>Full Name<b>${esc(d.profile.fullName)}</b></span><span>Date of Birth<b>${esc(d.profile.dob||"—")}</b></span><span>Gender<b>${esc(d.profile.gender||"—")}</b></span><span>Department<b>${esc(d.profile.department)}</b></span><span>Year / Section<b>${esc(d.profile.year)} / ${esc(d.profile.section)}</b></span><span>Address<b>${esc(d.profile.address||"—")}</b></span></div></div><div class="card"><h3>Announcements</h3>${d.announcements.map(a=>`<div class="notice"><b>${esc(a.title)}</b><p>${esc(a.body)}</p></div>`).join("")}</div></div></div>`}catch(x){toast(x.message,false)}}
async function staffDashboard(){try{const d=await api("/api/staff/dashboard");app.innerHTML=`<div class="dashboard"><div class="dash-head"><div><div class="eyebrow">STAFF WORKSPACE</div><h1>Academic Control Center</h1><p class="muted">View registered students and update academic records.</p></div><span class="status">● Authorized Staff</span></div><div class="stats"><div class="stat"><span class="label">Registered Students</span><b>${d.counts.students}</b><small>Student accounts</small></div><div class="stat"><span class="label">Staff Accounts</span><b>${d.counts.staff}</b><small>Authorized users</small></div><div class="stat"><span class="label">Subjects</span><b>${d.counts.courses}</b><small>Available courses</small></div><div class="stat"><span class="label">Departments</span><b>${d.counts.departments}</b><small>Active departments</small></div></div><div class="card callout"><div><div class="eyebrow">MARK ENTRY</div><h3>Enter marks for any registered student</h3><p class="muted">Select a student → choose subject → enter attendance, internal and external marks. The backend calculates total percentage, grade and CGPA automatically.</p></div><button class="btn" onclick="loadStudents()">Open Student Directory →</button></div><div class="table-wrap"><div class="table-title"><div><strong>Recent Student Records</strong><span class="muted">Reg ID, percentage and CGPA</span></div></div>${studentTable(d.recent)}</div></div>`}catch(x){toast(x.message,false)}}
function studentTable(rows){return `<table><thead><tr><th>Login / Reg ID</th><th>Student</th><th>Department</th><th>Year</th><th>Overall %</th><th>CGPA</th><th>Status</th><th></th></tr></thead><tbody>${rows.map(s=>`<tr><td><b>${esc(s.loginId)}</b></td><td>${esc(s.fullName)}<br><small class="muted">${esc(s.email)}</small></td><td>${esc(s.department)}</td><td>${esc(s.year)} / ${esc(s.section||"-")}</td><td>${Number(s.overallPercentage||0).toFixed(2)}%</td><td>${Number(s.cgpa||0).toFixed(2)}</td><td><span class="pill">${esc(s.status)}</span></td><td><button class="btn secondary mini" onclick="viewStudent(${s.id})">Enter Marks</button></td></tr>`).join("")}</tbody></table>`}
async function loadStudents(){try{const rows=await api("/api/staff/students");state.staffStudents=rows;app.innerHTML=`<div class="dashboard"><div class="dash-head"><div><div class="eyebrow">STUDENT DIRECTORY</div><h1>Registered Students</h1><p class="muted">${rows.length} student account(s) available to staff</p></div><button class="btn secondary" onclick="showPage('dashboard')">← Dashboard</button></div><div class="table-wrap"><div class="table-title"><div><strong>All Students</strong><span class="muted">Search by name, login ID or department</span></div><input id="studentSearch" oninput="filterStudents()" placeholder="Search..."></div><div id="studentTable">${studentTable(rows)}</div></div></div>`}catch(x){toast(x.message,false)}}
function filterStudents(){const q=studentSearch.value.toLowerCase();document.getElementById("studentTable").innerHTML=studentTable(state.staffStudents.filter(s=>(s.fullName+" "+s.loginId+" "+s.department).toLowerCase().includes(q)))}
async function viewStudent(id){try{const d=await api("/api/staff/students/"+id),courses=await api("/api/courses");state.courses=courses;app.innerHTML=`<div class="dashboard"><div class="dash-head"><div><div class="eyebrow">ACADEMIC RECORD</div><h1>${esc(d.student.fullName)}</h1><p class="muted">${esc(d.student.loginId)} · ${esc(d.student.department)} · ${esc(d.student.year)}</p></div><button class="btn secondary" onclick="loadStudents()">← Student List</button></div><div class="stats"><div class="stat"><span class="label">Overall Percentage</span><b>${d.academic.overallPercentage.toFixed(2)}%</b></div><div class="stat"><span class="label">CGPA</span><b>${d.academic.cgpa.toFixed(2)}</b></div><div class="stat"><span class="label">Attendance</span><b>${d.academic.attendance.toFixed(1)}%</b></div><div class="stat"><span class="label">Login ID</span><b style="font-size:18px">${esc(d.student.loginId)}</b></div></div><div class="two-col"><div class="card"><h3>Student Identity</h3><div class="detail-grid"><span>Full Name<b>${esc(d.student.fullName)}</b></span><span>DOB<b>${esc(d.student.dob||"—")}</b></span><span>Gender<b>${esc(d.student.gender||"—")}</b></span><span>Parent<b>${esc(d.student.parentName||"—")}</b></span><span>Phone<b>${esc(d.student.phone||"—")}</b></span><span>Email<b>${esc(d.student.email)}</b></span></div></div><div class="card"><h3>Enter / Update Marks</h3><form onsubmit="saveMarks(event,${id})"><label>Subject</label><select id="mCourse">${courses.map(c=>`<option value="${c.id}">${esc(c.code)} — ${esc(c.name)} (${c.credits} credits)</option>`).join("")}</select><div class="mark-grid"><div><label>Attendance %</label><input id="mAtt" type="number" min="0" max="100" step="0.1" required value="90"></div><div><label>Internal / 40</label><input id="mInt" type="number" min="0" max="40" step="0.5" required value="32"></div><div><label>External / 60</label><input id="mExt" type="number" min="0" max="60" step="0.5" required value="50"></div></div><div class="calc-preview"><span>Auto result</span><b id="markPreview">82 / 100 • A+ • 9 GP</b></div><button class="btn full">Save Marks & Recalculate →</button></form></div></div><div class="table-wrap"><div class="table-title"><div><strong>Current Academic Records</strong><span class="muted">Marks entered by staff</span></div></div><table><thead><tr><th>Subject</th><th>Attendance</th><th>Internal</th><th>External</th><th>Total %</th><th>Grade</th><th>Point</th></tr></thead><tbody>${d.courses.length?d.courses.map(c=>`<tr><td><b>${esc(c.course.code)}</b><br>${esc(c.course.name)}</td><td>${c.attendance}%</td><td>${c.internalMark}</td><td>${c.externalMark}</td><td>${c.totalMark}%</td><td><span class="pill">${esc(c.grade)}</span></td><td>${c.gradePoint}</td></tr>`).join(""):`<tr><td colspan="7">No marks entered yet.</td></tr>`}</tbody></table></div></div>`;updatePreview()}catch(x){toast(x.message,false)}}
function gradePreview(total){if(total>=90)return ["O",10];if(total>=80)return ["A+",9];if(total>=70)return ["A",8];if(total>=60)return ["B+",7];if(total>=50)return ["B",6];if(total>=40)return ["C",5];return ["RA",0]}
function updatePreview(){const a=document.getElementById("mInt"),b=document.getElementById("mExt"),p=document.getElementById("markPreview");if(!a||!b||!p)return;const total=Number(a.value||0)+Number(b.value||0),g=gradePreview(total);p.textContent=`${total.toFixed(1)} / 100 • ${g[0]} • ${g[1]} GP`;a.oninput=b.oninput=updatePreview}
async function saveMarks(e,id){e.preventDefault();try{const d=await api("/api/staff/students/"+id+"/marks",{method:"PUT",body:{courseId:Number(mCourse.value),attendance:Number(mAtt.value),internalMark:Number(mInt.value),externalMark:Number(mExt.value)}});toast(`${d.message} Overall: ${d.academic.overallPercentage.toFixed(2)}% • CGPA: ${d.academic.cgpa.toFixed(2)}`);viewStudent(id)}catch(x){toast(x.message,false)}}
async function profile(){try{const u=await api("/api/me");app.innerHTML=`<div class="dashboard"><div class="dash-head"><div><div class="eyebrow">ACCOUNT</div><h1>My Profile</h1><p class="muted">Update contact details. Login ID cannot be changed.</p></div></div><div class="login profile-login"><form onsubmit="saveProfile(event)"><label>Full name</label><input id="pName" value="${esc(u.fullName)}" required><label>Email</label><input id="pEmail" type="email" value="${esc(u.email)}" required><label>Phone</label><input id="pPhone" value="${esc(u.phone||"")}"><label>Login ID</label><input value="${esc(u.loginId)}" disabled><button class="btn full">Save Profile</button></form></div></div>`}catch(x){toast(x.message,false)}}
async function saveProfile(e){e.preventDefault();try{await api("/api/profile",{method:"PUT",body:{fullName:pName.value,email:pEmail.value,phone:pPhone.value}});toast("Profile updated successfully");showPage("profile")}catch(x){toast(x.message,false)}}
function showPage(p){layoutNav();if(p==="home")home();else if(p==="login")login();else if(p==="register")register();else if(p==="profile")profile();else if(p==="dashboard"){if(!state.user)login();else state.user.role==="student"?studentDashboard():staffDashboard()}}
layoutNav();showPage(state.user?"dashboard":"home");
