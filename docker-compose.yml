services:
  ajv-collegeconnect:
    build: .
    container_name: ajv-collegeconnect
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      JWT_SECRET: ajv-collegeconnect-production-secret
    volumes:
      - ajv_data:/app/data
    restart: unless-stopped
volumes:
  ajv_data:
